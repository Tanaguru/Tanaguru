Aria-Accordion=The design pattern is it well implemented ?
tablistRoleWithoutAriaMultiselectable=The main composant with <code>role="tablist"</code> hasn't an <code>aria-multiselectable</code> attribute.
tablistRoleWithAriaMultiselectableNotSetHasTrue=The main composant with <code>role="tablist"</code> has not an <code>aria-multiselectable</code> attribute set has true.
tabRoleWithoutAncestorWithRoleTabList=An element with <code>role="tab"</code> has not an ancestor with <code>role="tablist"</code> attribute.
tabRoleWithoutAnIdAttribute=An element with <code>role="tab"</code> attribute has not an <code>id</code> attribute.
tabRoleWithoutAnAriaSelectedAttribute=An element with <code>role="tab"</code> attribute has not an <code>aria-selected</code> attribute.
tablistRoleWithMultipleTabWithAriaSelectedSetHasTrue=There are at least two element with <code>aria-selected="true"</code> under the same main composant.
tabpanelRoleWithoutAncestorWithRoleTabList=An element with <code>role="tabpanel"</code> has not an ancestor with <code>role="tablist"</code> attribute.
tabpanelRoleWithoutAnAriaExpandedAttribute=An element with <code>role="tabpanel"</code> attribute has not an <code>aria-expanded</code> attribute.
tabpanelRoleWithoutAnAriaHiddenAttribute=An element with <code>role="tabpanel"</code> attribute has not an <code>aria-hidden</code> attribute.
tabpanelRoleWithoutAnAriaLabelledbyAttribute=An element with <code>role="tabpanel"</code> attribute has not an <code>aria-labelledby</code> attribute.
tabpanelRoleWithAnAriaHiddenAndExpandedAttributeSetToSameValue=An element with <code>role="tabpanel"</code> has an attribute <code>aria-expanded</code> and an attribute <code>aria-hidden</code> set with the same value.
tabpanelRoleUndefinedReferencedIdHeader=An element with <code>role="tabpanel"</code> reference with <code>aria-labelledby</code> an <code>id</code> header not found.
tabpanelRoleMultipleReferencedIdHeader=Multiple element with <code>role="tabpanel"</code> reference the same header use <code>aria-labelledby</code> attribute.
